require 'spec_helper'

describe "Editing a discipline" do
  it "updates the discipline and shows it in the listing page" do
    # discipline = create(:discipline)

    # visit disciplines_url

    # find('tr', text: discipline.name).click_link('Edit')

    # expect(current_path).to eq(edit_discipline_path(discipline))

    # expect(find_field("Name").value).to eq(discipline.name)


    # fill_in 'Name', with: "New name"

    # click_button "Update Discipline"

    # expect(current_path).to eq(disciplines_path)

    # expect(page).to have_text("New name")

  end
end
